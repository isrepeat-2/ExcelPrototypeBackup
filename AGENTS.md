# Agent Instructions

At the start of every new session (even without a user prompt), do this:
1. Read `Codex/instructions.txt`.
2. Read `Codex/session_summary.txt`.

Notes
- Screenshots are in `screenshots/`. If the user says "посмотри скрин" or "на скрине", open all images in that folder.
